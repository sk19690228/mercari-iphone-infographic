# 固定条件・新着自動検索版のGAS更新手順（v40）

この版はGmailを読み込みません。指定済みのメルカリ検索URLをGAS側で管理し、アプリを開くたびに新着商品を取得します。

## 1. Code.gsを更新

1. Apps Scriptの既存プロジェクトを開きます。
2. `Code.gs` をリポジトリの最新版で全置換して保存します。
3. 「プロジェクトの設定」→「スクリプト プロパティ」を開きます。
4. `APP_TOKEN` に、アプリの「中継用トークン」と同じ長い文字列を設定します。
5. 旧版の `MAIL_QUERY` は不要なので削除できます。

Gmail権限の承認や `authorizeGmail` の実行は不要です。

## 2. ウェブアプリを再デプロイ

1. 「デプロイ」→「デプロイを管理」を開きます。
2. ウェブアプリの鉛筆アイコンを押します。
3. バージョンを「新バージョン」にします。
4. 「次のユーザーとして実行」は「自分」を選びます。
5. 「アクセスできるユーザー」は「全員」を選びます。
6. デプロイし、末尾が `/exec` のURLをコピーします。

## 3. アプリで使う

1. 「URL取得・選定条件」を開きます。
2. GASウェブアプリURLと `APP_TOKEN` と同じ中継用トークンを入力します。
3. 「URL取得用GASをテスト」を押し、`メルカリURL取得中継 v40` と表示されることを確認します。
4. アプリを開き直すと自動検索が始まります。
5. 必要に応じて「新着商品を再検索・おすすめ決定」を押します。

公開アプリはGmailの受信トレイ、メール本文、処理済みラベルへアクセスしません。
