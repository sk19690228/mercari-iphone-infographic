# プロジェクト引き継ぎ資料：Mercari iPhoneインフォグラフィック作成アプリ

このファイルは、Claudeとの開発を別のAI（ChatGPT等）に引き継ぐための仕様書です。
**このファイルと一緒に、最新の `index.html` の中身も必ずコピー＆ペーストまたはアップロードしてください。**
このMarkdownは「何を作っているか・なぜそうなっているか」の説明で、実際のコードは`index.html`に全部入っています。

---

## 1. プロジェクト概要

Mercariのiphone中古商品ページのURLを貼るだけで、
- 商品情報を自動取得
- 中古相場・Amazon整備済み品価格と比較した5段階評価グラフを自動生成
- Xポスト用のインフォグラフィック画像を生成
- 投稿文・アフィリエイトPR文をワンクリックでコピー

できる「単一HTMLファイルで完結するPWA」。サーバーは使わず、ブラウザだけで完結する設計方針。

## 2. デプロイ状況

- **GitHubリポジトリ**: `https://github.com/sk19690228/mercari-iphone-infographic` (Public)
- **公開URL（GitHub Pages）**: `https://sk19690228.github.io/mercari-iphone-infographic/`
- 更新方法：GitHub上で対象ファイルを開き、鉛筆アイコンで編集→中身を貼り替え→Commit。数十秒〜数分でPagesに反映される（反映されない場合は`?v=2`等をURL末尾に付けてキャッシュ回避）
- **注意**: このアプリはClaudeのチャット画面内のプレビュー枠（アーティファクト）ではネットワーク通信がブロックされ正しく動作しない。必ず上記の実際のURLをブラウザの通常タブで開いてテストすること。

## 3. ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体。HTML/CSS/JSすべて1ファイルに内包 |
| `manifest.json` | PWAマニフェスト（ホーム画面追加用） |
| `sw.js` | 最小構成のService Worker（オフラインキャッシュ） |
| `icon-192.png` / `icon-512.png` | PWAアイコン |
| `amazon-urls.json` | iPhone16シリーズのAmazon整備済み品URL登録データ（共有用、後述） |
| `mercari-avg-prices.json` | iPhone16シリーズの中古平均価格の上書きデータ（共有用、後述） |
| `README.md` | セットアップ手順 |

`mercari-infographic.html`という単体版ファイルも別途存在する（GitHubを使わずClaudeのアーティファクトや手元のブラウザで直接開くための、manifest/service worker登録を除いた同一内容のコピー）。**index.htmlを更新したら、このファイルにも同じ変更を反映する運用にしていた。**

## 4. 画面構成（セクション番号は実装上の見出し文言と一致）

1. **① 商品URLを取得**：MercariのURL入力→「ページ情報を取得」ボタン
2. **② 内容を確認・修正**：商品タイトル、出品価格、機種名(プルダウン)、メモリ容量(プルダウン)、中古平均相場(自動表示＋手動上書き)、色、商品状態(プルダウン)、バッテリー最大容量(%)、Amazon整備済み品の最低価格(手入力)、Amazon検索リンク(自動生成)、Amazon商品ページURL＋取得ボタン(試験的)
3. **③ グラフのプレビュー**：4項目（価格・メモリ容量・商品の状態・バッテリー）の横棒グラフを自動計算・自動描画
4. **④ 投稿文をコピー**：商品タイトル\n出品価格\nAmazon整備済み品最安値、の3行テキストを1クリックでクリップボードにコピー
5. **⑤ PR表記・アフィリエイトリンク**：メルカリ／Amazonのアフィリエイトリンク入力欄＋絵文字入りPR定型文を1クリックでコピー
6. **⑥ Xポスト用画像を生成**：Canvas APIで1200×800pxのダーク配色インフォグラフィックを生成し、ダウンロード可能
7. **🔗 GitHub連携設定**：GitHub Personal Access Tokenの入力欄（保存されない、セッション限り）、リポジトリ所有者/リポジトリ名
8. **⚙️ Amazon整備済み品URL登録（iPhone16シリーズ）**：iPhone16シリーズ全16通り（機種×容量）のAmazon商品URLを登録。「共有用JSONをコピー」または「GitHubに保存」ボタンで`amazon-urls.json`に反映
9. **📊 中古平均価格一覧（iPhone16シリーズ）**：同じく全16通りの中古平均価格を一覧編集。「共有用JSONをコピー」または「GitHubに保存」ボタンで`mercari-avg-prices.json`に反映

## 5. スコア計算ロジック（重要・仕様として確定済み）

### 価格（出品価格 と 中古平均価格 の差額 diff = 出品価格 − 平均価格）
| スコア | 条件 | 表示ラベル |
|---|---|---|
| 5 | diff ≤ -5001円 | 格安 |
| 4 | -5000円 ≤ diff ≤ -3001円 | 割安 |
| 3 | -3000円 ≤ diff ≤ -1001円 | ちょい安 |
| 2 | -1000円 ≤ diff ≤ +1000円 | 相場並み |
| 1 | +1001円 ≤ diff ≤ +3000円 | ちょい高 |
| 0 | diff ≥ +3001円 | 割高 |

平均価格が未入力、または出品価格が未入力の場合はスコア2.5（中間）・灰色・バッジ「?」で表示（判定不能の統一表現）。

### メモリ容量
5=512GB, 4=256GB, 3=128GB, 2=64GB, 1=32GB, 0=16GB以下

### 商品の状態
5=新品、未使用／4=未使用に近い／3=目立った傷や汚れなし／2=やや傷や汚れあり／1=傷や汚れあり／0=全体的に状態が悪い

### バッテリー最大容量
5=100%／4=95〜99%／3=90〜94%／2=85〜89%／1=80〜84%／0=79%以下。
不明な場合はスコア2.5・灰色・バッジ「?」（価格と同じ統一表現）。手入力欄には「情報：無」を自動セット。

### 横棒グラフの描画ルール
- スコア0〜5を0〜5段階のグラデーション色（赤→緑、`SCORE_COLORS`配列）でバー幅に反映
- 「判定不能」はスコア2.5固定・グレー・バー内バッジ「?」で統一表示

## 6. Mercariページ取得の実装知見（重要・試行錯誤の結果わかったこと）

- CORSの制約で直接fetchできないため、複数の公開プロキシを順に試すフォールバック方式（`fetchHtmlViaProxies`関数）。使用中: `api.allorigins.win/raw` → `corsproxy.io` → `api.codetabs.com/v1/proxy`
- **価格情報はタイトルや説明文には入っておらず、`<meta property="product:price:amount" content="...">` というメタタグに直接入っている。** これが分かるまで正規表現で何度も失敗した。
- `og:description`は宣伝用の定型文のみで、実際の商品説明（バッテリー%、傷の状態など）は**ページ本文**にある。ページ全体を検索対象にすると関連商品やフッターの文字列を誤検出するため、`"商品の説明"`という文字列から`"出品者"`という文字列の直前までの範囲だけを抽出して検索対象にしている（ノイズ除去）。
- 取得したページのタイトルが「メルカリ - 日本最大のフリマサービス」等の汎用タイトルだった場合は、商品が削除/売り切れ後にトップページへリダイレクトされた可能性が高いため、エラーとして扱うガードを入れている（`genericTitlePatterns`）。
- 画像の読み込みは`images.weserv.nl`（画像プロキシ）を優先し、失敗時にallorigins→元URL、の順にフォールバック（`loadImageViaProxy`関数）。通常のfetchより画像専用プロキシの方が安定した。
- fetchのタイムアウトに`AbortController`の`signal`を使うと、Claudeのアーティファクト環境（postMessage経由でfetchを中継する仕組み）で「AbortSignal object could not be cloned」というエラーが出た。**`AbortController`は使わず、`Promise.race`でタイムアウトを実装している。**

## 7. Amazon連携について（未解決・要検討事項）

- Amazon Product Advertising API（PA-API）およびその後継のCreators APIは、いずれもリクエストへの電子署名（Credential Secret）が必須。**秘密鍵を静的なHTMLファイル（GitHub Pages等）に埋め込むと誰でも盗める状態になるため、クライアントサイドのみでは実装不可能**という結論に至っている。
- 代替案として検討済み・未着手のもの：
  1. Google Apps Script（GAS）を無料の中継サーバーとして使い、そこにCredential ID/Secretを保管してCreators APIを呼び出す方式（実装例が複数のブログで確認できている）
  2. 現状の妥協案：Amazon整備済み品の商品ページURLを試験的にスクレイピング（同じプロキシ経由でfetch→JSON-LD/価格クラスから抽出）。**Amazonはボット検知が厳しく、Mercariより成功率が低いことが分かっている**（`extractAmazonPrice`関数、`fetchAmazonBtn`ボタン、試験的機能として実装済み）
  3. 現在の運用：iPhone16シリーズに限り、Amazon整備済み品ページURLを事前登録しておき、機種・容量が一致したら自動でそのURLの価格取得を試みる仕組み（`amazonRegistry`, `autoFillAmazonFromRegistry`関数）

**次にAI側で検討・実装してほしいこと（ユーザーの意向）**：GASを使った本格的なCreators API連携の実装。ユーザー側でAmazonアソシエイト管理画面から認証情報（Credential ID/Secret）を発行する必要があり、これはAI側では代行できない。

## 8. GitHubへのデータ書き込み方式（セキュリティ設計の理由）

`amazon-urls.json`と`mercari-avg-prices.json`は、複数端末（スマホ・自宅PC）でAmazon URL登録・中古相場を共有するための仕組み。

- **読み込み**：アプリ起動時に`fetch('./amazon-urls.json')`等で読み込むだけなので認証不要、安全
- **書き込み**：GitHub Contents API（`PUT https://api.github.com/repos/{owner}/{repo}/contents/{path}`）を使い、ユーザーがその場で入力したPersonal Access Tokenで認証。**トークンはコード内に一切保存せず、入力フォームに毎回入力する運用**（タブを閉じると消える）。これにより公開リポジトリのソースコードにトークンが漏れることを防いでいる（`saveJsonToGithub`関数）
- 上記が使いたくない場合の代替：「共有用JSONをコピー」ボタンでクリップボードにコピーし、GitHub上で手動貼り替え

## 9. 中古相場データの出典・更新方法

`PRICE_TABLE`という定数オブジェクトに、iPhone SE(第2世代)〜iPhone 17 Pro Max, iPhone Air, 16e/17e等、機種×容量ごとの中古平均価格（円）をハードコードしている。

- 出典：ムスビー（musbi.net）の「90日間平均取引価格」（ランクA＝美品基準）。2026年7月30日時点のデータを元にリサーチして埋め込んだもの。
- **これは静的なスナップショットであり、自動更新はされない。** 相場は変動するため、定期的な見直しが必要（README内にもその旨記載）。
- iPhone16シリーズのみ、`mercari-avg-prices.json`経由でこの値を上書きできる仕組みが後付けされている（セクション9参照）。他機種にはこの上書き機能はまだ実装していない。

## 10. 今後の拡張候補（会話の中で出た検討事項、未着手）

- Amazon Creators API + GAS連携の本格実装（セクション7参照）
- iPhone16シリーズ以外の機種にも、Amazon URL登録・中古相場上書きの仕組みを拡張するか検討
- 中古相場データの定期的な自動更新の仕組み化（現状は手動リサーチ→ハードコードの更新のみ）

## 11. 開発の背景・方針（引き継ぐAIが理解しておくべきこと）

- ユーザーは以前、Mercari等5プラットフォームを対象にした「使用済みiPhoneアフィリエイト自動投稿ツール」の大きな構想を持っていたが、**Mercariの利用規約に抵触しうる自動スクレイピングのリスクを懸念**し、フリマモニターアプリの通知連携等、よりリスクの低い方式に方針転換した経緯がある。本アプリはその流れの中で「単発でURLを手動貼り付けする、低リスクな軽量ツール」として位置づけられている。**大量URLの自動連続取得・自動投稿のような機能は意図的に実装していない。**
- Amazonアフィリエイト・メルカリアンバサダーの両方について、PR表記（景品表示法対応）を明示的に含めるようユーザーから指示されている（⑤セクションの「【PR】メルカリアンバサダー」「【PR】amazonアフィリエイト」表記）。この方針は今後の機能追加でも継続すること。
- 実装は常に「まず動くものを手入力ベースで作る→自動化を試みる→ダメなら手動フォールバックを残す」という段階的な進め方をしてきた（Mercari自動取得、Amazon価格取得、GitHub保存、いずれも同じパターン）。
