# Gmail取得機能の更新手順（v35）

## 1. Apps Scriptのコードを更新

1. Apps Scriptの「Mercari Gmail通知中継」プロジェクトを開きます。
2. `コード.gs` の内容をすべて削除します。
3. このフォルダーの `Code.gs` をすべてコピーし、`コード.gs` へ貼り付けて保存します。
4. `APP_TOKEN` はコードへ直接書かず、「プロジェクトの設定」→「スクリプト プロパティ」に設定します。

| プロパティ | 値 |
| --- | --- |
| `APP_TOKEN` | PWAの「中継用トークン」と同じ長い文字列 |
| `MAIL_QUERY` | 任意。通常は未設定のままにします |

## 2. Gmail権限を確認

Apps Script上部の関数一覧で `authorizeGmail` を選び、「実行」を押します。権限確認が表示された場合は、Gmailへのアクセスを許可してください。

## 3. 必ず新バージョンを再デプロイ

コードを保存しただけでは、現在のウェブアプリへ反映されません。

1. 「デプロイ」→「デプロイを管理」を開きます。
2. 使用中のウェブアプリの鉛筆アイコンを押します。
3. バージョンで「新バージョン」を選びます。
4. 「デプロイ」を押します。
5. `/exec` で終わるURLがPWAの「GASウェブアプリURL」と同じことを確認します。

ウェブアプリの設定は「次のユーザーとして実行: 自分」「アクセスできるユーザー: 全員」にします。

## 4. PWAで確認

1. 公開ページを再読み込みします。古い表示が残る場合は、URL末尾に `?v=35` を付けて開きます。
2. 「Gmail接続をテスト」を押します。
3. 「接続できました」と直近の対象スレッド数が表示されることを確認します。
4. 「新着通知メールを検索・おすすめ決定」を押します。

検索結果には、確認メール数、未処理メール数、処理済みスキップ数、取得済み商品のスキップ数、URL抽出失敗数が表示されます。

## 取得できない場合

- 「確認メール: 0通」: 「メールを遡る日数」を30日にして再実行します。`MAIL_QUERY`を設定済みなら、いったん削除して再デプロイします。
- 「未処理メール: 0通」: 「メール処理履歴をリセット」を押して再実行します。
- 「URL抽出失敗」が増える: 対象メール本文にメルカリの商品URLがあるか確認します。最新版は転送メールとURLエンコード済みリンクにも対応しています。
- タイムアウト: Apps Scriptが最新版として再デプロイされているか確認します。画面側の待機時間はv35で最大240秒へ延長済みです。

取得に失敗したメールは処理済みにしないため、設定を直した後に同じメールを再確認できます。
