# Gmail通知取得版 セットアップ手順

この版は、メルカリの検索結果ページを巡回しません。
スマホのPWAで「新着通知メールを検索・おすすめ決定」を押した時だけ、Google Apps Script（GAS）がGmailのメルカリ通知を確認します。

## 全体の流れ

1. メルカリでiPhone 16シリーズの検索条件を保存する
2. 新着通知をGmailで受信する
3. PWAの検索ボタンを押す
4. GASが未処理メールから商品URLを抽出する
5. 商品情報を取得し、中古平均価格と比較する
6. おすすめ1位をインフォグラフィック欄へ反映する

定期トリガー、外部有料API、Playwrightは不要です。

---

## 1. Apps ScriptへCode.gsを貼り付ける

1. `https://script.google.com/` を開く
2. 「新しいプロジェクト」をクリック
3. プロジェクト名を `Mercari Gmail通知中継` などへ変更
4. 初期コード `function myFunction() {}` をすべて削除
5. 同梱の `Code.gs` を開き、内容をすべてコピー
6. Apps Scriptの `コード.gs` に貼り付ける
7. 保存する

コード内に `PARSE_API_KEY` や `api.parse.bot` がないことを確認してください。

---

## 2. APP_TOKENを設定する

1. 左側の歯車「プロジェクトの設定」を開く
2. 「スクリプト プロパティ」まで下へ移動
3. 「スクリプト プロパティを追加」をクリック
4. 次を登録

| プロパティ | 値 |
|---|---|
| `APP_TOKEN` | 自分で決めた長い文字列 |

例：

```text
iphone-mail-2026-X7Qa91pLm3Nz82dFhV0m
```

この値は、後でPWAの「中継用トークン」にも同じものを入力します。

### Gmail検索条件を限定する場合（任意）

通常は設定不要です。メルカリ以外のメールまで拾う場合だけ、次のスクリプトプロパティを追加します。

| プロパティ | 値の例 |
|---|---|
| `MAIL_QUERY` | `label:メルカリ新着` |

この場合、Gmail側でメルカリ通知に「メルカリ新着」ラベルが付くフィルタを作ってください。

---

## 3. Gmail権限を許可する

1. Apps Script上部の関数選択欄で `authorizeGmail` を選択
2. 「実行」をクリック
3. Googleアカウントを選択
4. 警告画面が出た場合は「詳細」→プロジェクト名へ移動
5. Gmailへのアクセスを許可

この処理は初回のみです。

`authorizeGmail` は受信トレイの未読件数を確認するだけで、メールを送信・削除しません。

---

## 4. ウェブアプリとしてデプロイする

1. 右上「デプロイ」→「新しいデプロイ」
2. 種類の選択→「ウェブアプリ」
3. 次のように設定

- 説明：`Gmailメルカリ通知中継`
- 次のユーザーとして実行：`自分`
- アクセスできるユーザー：`全員`

4. 「デプロイ」をクリック
5. `/exec` で終わるウェブアプリURLをコピー

例：

```text
https://script.google.com/macros/s/XXXXXXXXXXXXXXXX/exec
```

コードを後から修正した場合は、

「デプロイ」→「デプロイを管理」→鉛筆アイコン→「新バージョン」→「デプロイ」

と進めます。保存だけでは公開版に反映されません。

---

## 5. GitHub PagesへPWAを上書きする

同梱の次のファイルを、GitHubリポジトリ直下へ上書きします。

```text
index.html
manifest.json
sw.js
icon-192.png
icon-512.png
amazon-urls.json
mercari-avg-prices.json
```

`Code.gs` はGitHubではなくApps Scriptへ貼り付けます。

反映後はキャッシュ回避のため、公開URLの末尾に `?v=8` を付けて開いてください。

```text
https://sk19690228.github.io/mercari-iphone-infographic/?v=8
```

PWAをホーム画面へ追加済みの場合は、一度削除して再追加すると確実です。

---

## 6. PWAへGASを設定する

1. PWAを開く
2. ①「Gmailのメルカリ新着通知を取得」の「メール取得・選定条件」を開く
3. 「GASウェブアプリURL」に `/exec` URLを入力
4. 「中継用トークン」に `APP_TOKEN` と同じ値を入力
5. 「Gmail接続をテスト」を押す
6. 「接続できました」と表示されたら完了

---

## 7. 実際の使い方

1. Gmailへメルカリ新着通知が届く
2. PWAの「新着通知メールを検索・おすすめ決定」を押す
3. 未処理メールだけを確認
4. 商品URL、タイトル、価格、画像、説明を抽出
5. iPhone 16シリーズと判定できた商品を候補へ追加
6. 中古平均価格と比較し、おすすめ1位を③へ反映
7. 内容を確認してインフォグラフィックを生成

処理済みメールには、Gmail上で次のラベルが付きます。

```text
iPhoneインフォ/処理済み
```

定期実行はありません。検索ボタンを押さない限り、GASはGmailを確認しません。

---

## Yahoo!メールで通知を受け取っている場合

GASの `GmailApp` はYahoo!メールを直接確認できません。
次のどちらかにします。

- メルカリの登録メールアドレスをGmailへ変更する
- Yahoo!メールからメルカリ通知をGmailへ自動転送する

Gmailに届いた転送メールも、本文にメルカリの商品リンクが含まれていれば取得対象になります。

---

## 0件になる場合

### 「確認メール: 0通」

- Gmailにメルカリ通知が届いているか確認
- 「メールを遡る日数」を30日に変更
- メルカリ通知が別のGoogleアカウントへ届いていないか確認
- `MAIL_QUERY`を設定している場合は、一度削除して再デプロイ

### 「商品URLを抽出できなかったメール」

通知メールの形式が想定と異なる可能性があります。
「メール処理履歴をリセット」を押した後、対象メールの通常表示スクリーンショットを確認して抽出処理を調整します。

### 同じメールを再確認したい

PWAの「メール処理履歴をリセット」を押します。
Gmailのラベルは残りますが、GASの処理履歴が消えるため再取得できます。

### 商品ページ詳細を取得できない

GASが商品ページへのアクセスを制限された場合は、メール本文に含まれるタイトル・価格・画像を代わりに使います。情報不足の商品は候補から除外されることがあります。

---

## セキュリティ

- `APP_TOKEN`は推測されにくい長い文字列にする
- トークンをSNSや公開リポジトリへ掲載しない
- `Code.gs`内にGmailのパスワードを書く必要はない
- GASはデプロイ所有者のGmailだけを確認する
- メール本文や商品データを外部APIへ送信しない
